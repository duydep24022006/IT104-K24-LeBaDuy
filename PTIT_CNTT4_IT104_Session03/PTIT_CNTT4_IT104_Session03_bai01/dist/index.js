"use strict";
let userName = "duy";
let age = 19;
let job = "dev";
const renderUser = (userName, age, job) => {
    console.log(`        user name:${userName}
        age:${age}
        job:${job}`);
};
renderUser(userName, age, job);
